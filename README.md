# Some random ocp (openshift) tools

## get-ocp-upgrade-paths

Determine next possible version to upgrade to given a version.
Used from CLI to check upgrade path. Useful when OpenShift cluster itself is not online.
